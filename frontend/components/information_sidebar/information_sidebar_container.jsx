import { connect } from 'react-redux';

import InformationSidebar from './information_sidebar';

const mapStateToProps = (state) => {
  return {

  }
}

const mapDispatchToProps = (dispatch) => {
  return {

  }
}

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(InformationSidebar)
